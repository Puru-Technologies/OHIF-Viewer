import { id } from './id';
import getCustomizationModule from './getCustomizationModule';

const puruBrandingExtension = {
  id,
  getCustomizationModule,
};

export default puruBrandingExtension;
export { id };
